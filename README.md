# Digits Recognition using Convolutional Neural Network (2 Hidden Layers, 16 nodes per layer)

Using only Numpy, CNN is modeled to recognition MNIST digits

Findings: Having more layers may not necessarily be good. Overfitting may occur, especially for simpler tasks like classifying 10 classes
